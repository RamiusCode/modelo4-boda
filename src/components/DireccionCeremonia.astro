<!-- ═══════════════════════════════════════════════════════════ --><!-- ⛪ SECCIÓN: CEREMONIA                                       --><!-- ═══════════════════════════════════════════════════════════ -->
<section class="relative w-full pt-4 pb-12 sm:py-20 md:py-24 lg:py-32">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
        <!-- 📇 CARD DE CEREMONIA - HORIZONTAL -->
        <div class="flex justify-center">
            <div
                class="relative w-full max-w-md sm:max-w-lg md:max-w-xl"
                id="card-ceremonia"
            >
                <!-- BOTÓN DECORATIVO SUPERIOR -->
                <div
                    class="absolute z-20"
                    style="top: -30px; left: 50%; transform: translateX(-50%);"
                >
                    <div class="w-16 h-16" id="boton-decorativo-ceremonia">
                        <img
                            src="/imagenes/boton.png"
                            alt="Decoración"
                            class="w-full h-full object-contain"
                        />
                    </div>
                </div>

                <div
                    class="relative rounded-2xl shadow-xl overflow-hidden"
                    style="background-color: #e8dcc8;"
                >
                    <!-- IMAGEN DE FONDO -->
                    <img
                        src="/imagenes/fondorojo.png"
                        alt="Fondo ceremonia"
                        class="absolute inset-0 w-full h-full object-cover opacity-90"
                    />

                    <!-- Contenido de la card - LAYOUT CON TÍTULO EXTENDIDO -->
                    <div
                        class="relative px-4 pt-14 pb-8 sm:px-8 sm:pt-16 sm:pb-10 md:px-10 md:pt-16 md:pb-12 z-10"
                    >
                        <!-- TÍTULO: CEREMONIA RELIGIOSA - OCUPA TODO EL ANCHO -->
                        <div class="mb-2">
                            <h2
                                class="font-great-vibes text-4xl sm:text-4xl md:text-5xl texto-ceremonia text-center"
                                id="titulo-ceremonia"
                            >
                                Ceremonia Religiosa
                            </h2>
                        </div>

                        <!-- CONTENIDO INFERIOR CON ICONO Y TEXTOS -->
                        <div class="flex items-center justify-between gap-4">
                            <!-- LADO IZQUIERDO: ICONO DE IGLESIA -->
                            <div class="flex-shrink-0 ml-4 sm:ml-6 md:ml-8">
                                <div
                                    class="w-24 h-24 sm:w-20 sm:h-20 md:w-24 md:h-24 icono-iglesia-color"
                                >
                                    <img
                                        src="/imagenes/iglesia.png"
                                        alt="Iglesia"
                                        class="w-full h-full object-contain"
                                        id="icono-iglesia"
                                    />
                                </div>
                            </div>

                            <!-- LADO DERECHO: TEXTOS Y BOTÓN -->
                            <div class="flex-1 text-center">
                                <!-- NOMBRE DE LA PARROQUIA -->
                                <div class="mb-2">
                                    <p
                                        class="font-crimson text-xl sm:text-lg md:text-xl texto-ceremonia font-semibold"
                                        id="nombre-parroquia"
                                    >
                                        Parroquia "Sagrado Corazón de Jesús"
                                    </p>
                                </div>

                                <!-- DIRECCIÓN -->
                                <div class="mb-2 leading-tight">
                                    <p
                                        class="font-crimson text-xl sm:text-sm md:text-base texto-ceremonia"
                                        id="direccion-parroquia"
                                    >
                                        Av. 6 de Agosto N° 2730,<br />
                                        esquina Calle Fernando Guachalla
                                    </p>
                                </div>

                                <!-- HORA -->
                                <div class="mb-4">
                                    <p
                                        class="font-crimson text-xl sm:text-base md:text-lg leading-tight texto-ceremonia font-semibold"
                                        id="hora-ceremonia"
                                    >
                                        Hora: 15:00
                                    </p>
                                </div>

                                <!-- BOTÓN VER UBICACIÓN -->
                                <div class="mt-4">
                                    <a
                                        href="https://maps.app.goo.gl/qhVmESGg5jGdRhGy8"
                                        target="_blank"
                                        rel="noopener noreferrer"
                                        class="inline-block px-4 py-2 sm:px-6 sm:py-2.5 border-2 rounded-full font-crimson text-base sm:text-sm uppercase tracking-wider transition-all duration-300 btn-ubicacion-ceremonia font-semibold"
                                        id="btn-ubicacion"
                                    >
                                        VER UBICACIÓN
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- ═══════════════════════════════════════════════════════════ -->
<!-- 🎬 ANIMACIONES CON GSAP                                     -->
<!-- ═══════════════════════════════════════════════════════════ -->
<script>
    import { gsap } from "gsap";
    import { ScrollTrigger } from "gsap/ScrollTrigger";

    gsap.registerPlugin(ScrollTrigger);

    if (typeof window !== "undefined") {
        document.addEventListener("DOMContentLoaded", () => {

            const tl = gsap.timeline({
                scrollTrigger: {
                    trigger: "#card-ceremonia",
                    start: "top 80%",
                    toggleActions: "play none none reset",
                },
            });

            // Todos arrancan al mismo tiempo, cada uno con su propio movimiento
            tl.fromTo(
                "#boton-decorativo-ceremonia",
                { opacity: 0 },
                { opacity: 1, duration: 1.2, ease: "sine.out" },
                0
            )
            .fromTo(
                "#card-ceremonia > div",
                { opacity: 0, y: 40 },
                { opacity: 1, y: 0, duration: 1.3, ease: "power2.out" },
                0
            )
            .fromTo(
                "#titulo-ceremonia",
                { opacity: 0, y: -22 },
                { opacity: 1, y: 0, duration: 1.2, ease: "power2.out" },
                0
            )
            .fromTo(
                "#icono-iglesia",
                { opacity: 0, x: -28 },
                { opacity: 1, x: 0, duration: 1.2, ease: "power2.out" },
                0
            )
            .fromTo(
                "#nombre-parroquia",
                { opacity: 0, x: 28 },
                { opacity: 1, x: 0, duration: 1.2, ease: "power2.out" },
                0
            )
            .fromTo(
                "#direccion-parroquia",
                { opacity: 0, y: 22 },
                { opacity: 1, y: 0, duration: 1.2, ease: "power2.out" },
                0
            )
            .fromTo(
                "#hora-ceremonia",
                { opacity: 0, y: 22 },
                { opacity: 1, y: 0, duration: 1.2, ease: "power2.out" },
                0
            )
            .fromTo(
                "#btn-ubicacion",
                { opacity: 0 },
                { opacity: 1, duration: 1.3, ease: "sine.out" },
                0
            );
        });
    }
</script>

<!-- ═══════════════════════════════════════════════════════════ -->
<!-- 🎨 ESTILOS CSS                                              -->
<!-- ═══════════════════════════════════════════════════════════ -->
<style>
    /* Color de textos #122a4d */
    .texto-ceremonia {
        color: #122a4d;
    }

    /* Color del icono de iglesia #122a4d */
    .icono-iglesia-color {
        filter: brightness(0) saturate(100%) invert(13%) sepia(31%)
            saturate(1876%) hue-rotate(186deg) brightness(95%) contrast(96%);
    }

    /* Botón con color #122a4d */
    .btn-ubicacion-ceremonia {
        color: #122a4d;
        border-color: #122a4d;
    }

    /* Efecto hover en la card */
    #card-ceremonia > div {
        transition: all 0.4s ease;
        transform-style: preserve-3d;
    }

    /* #card-ceremonia > div:hover {
        transform: translateY(-8px);
        box-shadow: 0 10px 10px rgba(18, 42, 77, 0.3);
    } */
</style>
